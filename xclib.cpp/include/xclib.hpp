#pragma once

#include <string>
#include <vector>
#include <Windows.h>

#include "xcliball.h"

namespace xc
{

	void get_frame_grabber(
		const std::string& format_file_path,
		const std::vector<int>& ports)
	{

	}
}